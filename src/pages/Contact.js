import React from 'react';
import Typography from '@mui/material/Typography';

function Contact() {
    return (
        <div>
            <Typography variant = "h4" style={{padding: '0.5em'}}>Contact Page</Typography>
            <Typography variant = "body">Get in touch with us through the Contact Page.</Typography>
        </div>
    );
}

export default Contact;
